# Полный аудит sOn Messenger — 2026-04-10

6 параллельных аудиторов: Security, Frontend, Architecture, Tests, Database/API, Performance/DevOps.

---

## Сводка по severity

| Severity | Security | Frontend | Architecture | DB/API | Performance | Всего |
|---|---|---|---|---|---|---|
| **Critical** | 4 | 6 | 3 | 8 | 3 | **24** |
| **High** | 6 | 14 | 4 | 10 | 4 | **38** |
| **Medium** | 6 | — | 8 | 5 | 5 | **24** |
| **Low** | 4 | — | 5 | — | 3 | **12** |

---

## TOP-20 CRITICAL — Исправлять немедленно

### Security
| # | Проблема | Файл |
|---|---|---|
| C-S1 | Нет rate limit на `POST /api/auth/phone/send-code` — SMS DoS | index.js:3107 |
| C-S2 | `Math.random()` для OTP вместо `crypto.randomInt()` | index.js:3122 |
| C-S3 | IDOR: `GET /api/polls/:pollId` без проверки membership | index.js:2422 |
| C-S4 | Hardcoded LiveKit secrets в fallback (`devkey`/`secret`) | index.js:1556 |

### Frontend
| # | Проблема | Файл |
|---|---|---|
| C-F1 | encryptForSend мьютекс: resolve утечка → deadlock | secretChatStore.ts:240 |
| C-F2 | ratchetDecrypt без мьютекса — race condition, E2EE ломается | secretChatStore.ts:270 |
| C-F3 | WebRTC: TURN credentials не применяются (только STUN) | webrtc.ts:85 |
| C-F4 | E2EE сессии не восстанавливаются при cookie-restore (masterKey) | keyStore.ts:64 |
| C-F5 | MessageRow useEffect без deps — бесконечный цикл ре-рендеров | ConversationScreen.tsx:90 |
| C-F6 | Файловые вложения не отправляются на сервер (только локально) | ConversationScreen.tsx:340 |

### Database/API
| # | Проблема | Файл |
|---|---|---|
| C-D1 | **2FA не проверяется при логине** — можно войти без TOTP | index.js:220 |
| C-D2 | Race condition в `invite/join` без транзакции | index.js:1099 |
| C-D3 | N+1 в `sendPushToOfflineMembers` (N запросов на push) | index.js:1970 |
| C-D4 | N+1 bulk insert prekeys (100 INSERT в цикле) | index.js:2070 |
| C-D5 | Pool БД без параметров (нет timeout, max=10 default) | db.js:18 |
| C-D6 | `messages.sender_id` без ON DELETE — блокирует удаление юзера | 001_baseline.sql |
| C-D7 | `chats.pinned_message_id` без FK — dangling reference | 002_phase2.sql |
| C-D8 | Нет индекса на `messages(reply_to)` — slow ON DELETE SET NULL | отсутствует |

### Performance/DevOps
| # | Проблема | Файл |
|---|---|---|
| C-P1 | Нет WS heartbeat — мёртвые TCP соединения висят часами | index.js:1710 |
| C-P2 | Redis не используется для rate-limit store (in-memory) | index.js:124 |
| C-P3 | Staging деплоится без CI проверки | deploy-staging.yml |

### Architecture
| # | Проблема | Файл |
|---|---|---|
| C-A1 | WebSocket connections in-memory — не масштабируется горизонтально | index.js:1671 |
| C-A2 | `broadcastToChat` делает DB-запрос при каждом сообщении | index.js:1932 |
| C-A3 | Health check в deploy.yml — неправильный порт 8080 | deploy.yml:41 |

---

## HIGH — Исправлять в ближайших спринтах

### Security (6)
- H-S1: SSRF `isPublicUrl()` — неполная блокировка 172.x.x.x + нет 169.254
- H-S2: Stack trace leak через `console.error(err)` — 20+ мест
- H-S3: Нет rate limit на `POST /api/auth/phone/verify` — bruteforce OTP
- H-S4: `DELETE /invite/:inviteId` — нет проверки admin прав
- H-S5: Нет валидации `media_url` в stories/channels — SSRF/XSS
- H-S6: `GET /api/og` без отдельного rate limit — DoS через внешние HTTP

### Frontend (14)
- H-F1: offline-режим → logout при reconnect (authStore)
- H-F2: WS reconnect без jitter — thundering herd
- H-F3: fetchMessages race при переключении чатов
- H-F4: callStore listener side effect при hot-reload
- H-F5: UrlPreviewCard — API-запрос при каждом mount (нет кеша)
- H-F6: setState во время рендера в InputBar
- H-F7: deleteChat rollback через fetchChats
- H-F8: updateSetting key: string — нет type safety
- H-F9: getMasterKey throws при cookie-restore
- H-F10: skippedKeys Map растёт неограниченно (Double Ratchet)
- H-F11: Typing indicator зависает при несоответствии display_name
- H-F12: window.confirm блокирует поток
- H-F13: VoiceMessage Audio-объект не освобождается при смене URL
- H-F14: Ненадёжный type guard для Attachment (`'url' in` вместо `.type ===`)

### Architecture (4)
- H-A1: server/index.js 3450 строк — нужно разбить на модули
- H-A2: 103 console.* вместо pino logger
- H-A3: Нет `withTransaction` утилиты — дублирование паттерна
- H-A4: staging CI prerequisite отсутствует

### Database/API (10)
- H-D1: `subscriber_count` race condition (channels)
- H-D2: `POST /api/channels/subscribe` без транзакции
- H-D3: `POST /api/chats/:id/forward` без транзакции
- H-D4: Нет `GET /api/users/:id` — профиль пользователя
- H-D5: Нет `GET /api/chats/:chatId` — один чат по ID
- H-D6: `authMiddleware` не проверяет `sessions.is_revoked`
- H-D7: CHECK constraints на role-колонках отсутствуют
- H-D8: Нет индекса `sessions(user_id, last_active_at)`
- H-D9: N+1 при добавлении участников в чат
- H-D10: N+1 при создании вариантов опроса

### Performance (4)
- H-P1: broadcastToChat DB-запрос при каждом событии
- H-P2: ~60 console.error вместо pino logger
- H-P3: Нет zero-downtime деплоя
- H-P4: livekit-client/webrtc.ts в статическом импорте App.tsx

---

## MEDIUM (24 issues)

**Security:** JWT в теле+cookie, SameSite lax, story view без проверки, MIME без magic bytes, trust proxy bypass, membership info leak

**Architecture:** API versioning, og_cache TTL, индексы display_name, libsodium аудит, react-window замена, ScyllaDB/Kafka в compose неиспользуемы, Docker layer caching

**DB/API:** Pagination нестабильна при совпадающих timestamps, sticker_count не обновляется, views_count через correlated subquery, нет POST /api/auth/refresh, нет admin delete messages

**Performance:** loading="lazy" на аватарах, server/Dockerfile без multi-stage, нет Brotli nginx, isChatMember DB на каждый typing

---

## Тестовое покрытие

| Метрика | Frontend | Backend |
|---|---|---|
| Тесты | 308 (Vitest) | 46 (Jest) |
| Statements | 38.49% | ~27% (оценка) |
| Порог CI | 35/30/25% | нет |

**Критические непокрытые пути:**
1. `crypto/keyStore.ts` — 0% (E2EE ключи)
2. `crypto/doubleRatchet.ts` ratchetDecrypt — нет теста приёмной стороны
3. `utils/webrtc.ts` — 0% (звонки)
4. `stores/callStore.ts` — 15% (нет unit-теста)
5. Backend: contacts, MFA, phone auth, sessions, channels, stories, stickers — 0 тестов

---

## Приоритетный план исправлений

### Phase 0 — Блокеры безопасности (1-2 дня)
1. C-D1: 2FA проверка при логине
2. C-S2: crypto.randomInt() для OTP
3. C-S1: authLimiter на phone/send-code
4. C-S3: IDOR в polls
5. C-S4: LiveKit secrets check на старте
6. C-P1: WS heartbeat/ping-pong

### Phase 1 — E2EE и критические баги (2-3 дня)
7. C-F1: Мьютекс encryptForSend fix
8. C-F2: Мьютекс для decryptReceived
9. C-F4: MasterKey restore при cookie-auth
10. C-F3: TURN credentials в WebRTC
11. C-F5: useEffect deps в MessageRow
12. C-F6: Файловые вложения → отправка на сервер

### Phase 2 — Database и производительность (2-3 дня)
13. C-D2: Транзакция в invite/join
14. C-D3: N+1 push tokens → один запрос
15. C-D4: Bulk insert prekeys
16. C-D5: Pool config (max, timeout)
17. C-D8: Индекс messages(reply_to)
18. C-P2: Redis store для rate-limit
19. C-A2: Cache participants для broadcastToChat

### Phase 3 — Остальные High (1 неделя)
- H-S1–H-S6: SSRF, stack traces, rate limits
- H-F1–H-F14: Frontend баги
- H-D1–H-D10: DB транзакции и индексы
- H-A1: Разбиение index.js на модули
